# Placeholders only; copy to an IGNORED .dev.vars after building the app scaffold.
# Do not put secret values in frontend variables, wrangler vars, issues, or chat.
# Actual production values belong in Cloudflare Worker secrets.
CANVAS_CLIENT_SECRET="REPLACE_WITH_INSTITUTION_ISSUED_SECRET"
TOKEN_ENCRYPTION_ACTIVE_KEY_B64="REPLACE_WITH_BASE64_OF_32_RANDOM_BYTES"
# Retain a prior key only while a stored connection still names its version.
# The reviewed runtime defines the key-ring serialization; never paste production values here.
TOKEN_ENCRYPTION_LEGACY_KEYS_JSON="REPLACE_WITH_LOCAL_TEST_ONLY_VERSIONED_KEYS"
# Opaque session IDs and one-time state must be generated randomly by the server.
# Add separate keys only if the reviewed session design actually needs them.
